- command: apt-get install -y python-apt
  tags: [configuration, packages]

- name: Install packages
  apt:
    pkg: "{{ item }}"
    state: latest
    update_cache: yes
  with_items:
    - mc
    - htop
    - curl
    - git
  tags: [configuration, packages]
